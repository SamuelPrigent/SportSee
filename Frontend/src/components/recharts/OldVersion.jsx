// Responsive Container exemple (how it was before change
{
  /* Activity of the week Exemple */
}
{
  /* <ResponsiveContainer width="100%" height="100%">
        <BarChart
          width={500}
          height={300}
          barGap={8}
          barCategoryGap="35%"
          data={activity}
          margin={{ top: 85, right: 48, bottom: 30, left: 48 }}
        >
          <CartesianGrid
            strokeDasharray="2 2"
            vertical={false}
            stroke={`${color.neutral200}`}
          />
          <XAxis
            dataKey="day"
            dy={16}
            tick={{ fontSize: 14, fontWeight: 500 }}
            padding={{ left: -36, right: -36 }}
            tickLine={false}
            stroke={`${color.neutral400}`}
          />
          <YAxis
            orientation="right"
            dx={48}
            yAxisId="kg"
            dataKey="kilogram"
            axisLine={false}
            tickLine={false}
            domain={["dataMin - 1", "dataMax + 2"]}
          />
          <YAxis
            yAxisId="cal"
            dataKey="calories"
            domain={[0, "dataMax + 50"]}
            hide={true}
          />
          <Tooltip
            offset={47}
            content={<ActivityTooltip />}
            cursor={{
              fill: "rgba(0, 0, 0, 0.1)",
            }}
          />
          <Bar
            yAxisId="kg"
            dataKey="kilogram"
            radius={[50, 50, 0, 0]}
            fill="#282D30"
          />
          <Bar
            yAxisId="cal"
            dataKey="calories"
            radius={[50, 50, 0, 0]}
            fill="#E60000"
          />
        </BarChart>
      </ResponsiveContainer> */
}
